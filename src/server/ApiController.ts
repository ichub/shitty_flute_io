/**
 * Created by bgu on 7/14/17.
 */

import * as express from "express";

export const ApiController = express.Router();

ApiController.get("/:composerToken", (req: express.Request, res: express.Response) => {

});